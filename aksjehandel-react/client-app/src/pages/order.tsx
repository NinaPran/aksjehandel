import { FC, PropsWithChildren } from "react";

export const Order: FC<PropsWithChildren> = (props) => {
    return (
        <div className="nisse">
           {/* https://stackoverflow.com/a/70342010 for å hente ut attributter fra knappeklikk  */}
        </div>
    );
}